#include "CppUTest/TestHarness.h"

TEST_GROUP(CLI) {
    void setup() {
        // Set up before each test
    }

    void teardown() {
        // Clean up after each test
    }
};

TEST(CLI, testHelloCommand) {
	FAIL("FAIL ME");
};
